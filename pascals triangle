# pascal's triangle
from math import factorial
n=int(input("Enter number: "))

def pascal(n):
    
    for i in range(n):
        
        for j in range(i+1):
            print(factorial(i)//(factorial(j)*factorial(i-j)),end=" ")
        print()
        
pascal(n)
